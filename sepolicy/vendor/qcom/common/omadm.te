# OMADM app (omadm_app is coredomain, declared in platform sepolicy).
# Access to vendor-owned resources stays in vendor sepolicy.
allow omadm_app vendor_radio_data_file:dir rw_dir_perms;
allow omadm_app vendor_radio_data_file:file create_file_perms;
